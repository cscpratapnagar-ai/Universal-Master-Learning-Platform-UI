import { Injectable } from '@angular/core'; import { HttpClient } from '@angular/common/http'; @Injectable({providedIn:'root'}) export class CertificateService{private base='http://localhost:8080/api/v1/certificates';constructor(private http:HttpClient){} verify(number:string){return this.http.get<any>(`${this.base}/verify/${encodeURIComponent(number)}`);} issue(courseId:string,userId:string){return this.http.post<any>(`${this.base}/courses/${courseId}/users/${userId}/issue`,{});}}